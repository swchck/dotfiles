# This file is autogenerated by the aliases tool.
# For more info about aliases type `aliases --help`
# or visit https://github.com/sebglazebrook/aliases

---
sshw:
  command: ssh a.bogomazov@192.168.73.100
unlock:
  command: sudo xattr -rd com.apple.quarantine
ttl_on:
  command: sudo sysctl -w net.inet.ip.ttl=65
ttl_off:
  command: sudo sysctl -w net.inet.ip.ttl=64
tracert:
  command: traceroute
path:
  command: echo $PATH | tr -s ":" "\n"
dl:
  command: cd ~/Downloads
g:
  command: git
upd:
  command: python3 ~/dotfiles/update/update.py
...: 
  command: ../..
