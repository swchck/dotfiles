# Homebrew init (macOS ARM, macOS Intel, Linux)
for brewpath in /opt/homebrew/bin/brew /usr/local/bin/brew /home/linuxbrew/.linuxbrew/bin/brew; do
    if [ -x "$brewpath" ]; then
        eval "$("$brewpath" shellenv)"
        break
    fi
done