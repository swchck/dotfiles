# Files to remove on `chezmoi apply`
# These are old locations that have been migrated

# Git config migrated to ~/.config/git/
.gitconfig
.gitaliases
.gitmessage
.gitignore
.gitconfig-personal
.gitconfig-work
.git-scripts

# Removed tools
.config/ghostty

# glab config migrated from macOS default (Library) to XDG (~/.config/glab-cli)
Library/Application Support/glab-cli

# Old /ship slash command — superseded by the ship skill (agents-v2 roster)
.claude/commands/ship.md
